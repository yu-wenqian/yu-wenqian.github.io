name: Lighthouse Badger

on:
  workflow_dispatch:

jobs:
  lighthouse-badger-easy:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Lighthouse Badger is disabled while the personal site is being customized."