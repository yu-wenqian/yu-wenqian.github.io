name: Check for broken links on site

on:
  workflow_dispatch:

jobs:
  check-links-on-site:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Site link checks are disabled while the personal site is being customized."