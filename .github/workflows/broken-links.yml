name: Check for broken links

on:
  workflow_dispatch:

jobs:
  link-checker:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Broken link checks are disabled while the personal site is being customized."