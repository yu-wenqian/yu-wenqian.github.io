name: Prettier code formatter

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Prettier check is disabled while the personal site is being customized."